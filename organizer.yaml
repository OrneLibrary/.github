repositories:
  default:
    dependency_security:
      alerts: true
      automatic_fixes: true
    features:
      has_issues: true
    branches:
      main:
        enforce_admins: true
        required_approving_review_count: 1
        required_status_checks:
          require_review: true
